<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eujoh.uoeapp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="ANDROID.PERMISSION.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="ANDROID.PERMISSION.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".User.UserProfile"></activity>
        <activity android:name=".Common.LoginSignUp.Disclaimer" />
        <activity android:name=".User.AddLostItem" />
        <activity android:name=".User.LostFound" />
        <activity android:name=".User.SchoolWeb" />
        <activity android:name=".User.StudentPortal" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".Common.LoginSignUp.SignUp3rdClass" />
        <activity android:name=".Common.LoginSignUp.SignUp2ndClass" />
        <activity android:name=".Common.LoginSignUp.SignUp" />
        <activity android:name=".Common.LoginSignUp.Login" />
        <activity android:name=".Common.LoginSignUp.ForgetPassword" />
        <activity android:name=".Common.LoginSignUp.MakeSelection" />
        <activity android:name=".Common.LoginSignUp.SetNewPassword" />
        <activity android:name=".Common.LoginSignUp.PasswordUpdateSuccessful" />
        <activity android:name=".Common.LoginSignUp.StudentStartUpScreen" />
        <activity android:name=".Common.LoginSignUp.VerifyOTP" />
        <activity android:name=".User.contacts" />
        <activity android:name=".User.announcements" />
        <activity android:name=".Common.OnBoarding" />
        <activity android:name=".User.UserDashboard" />
        <activity android:name=".User.MapsActivity" />
        <activity android:name=".Common.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>